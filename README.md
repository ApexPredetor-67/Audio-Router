# Audio-Router
This is a code to route different audio files to different pairs of earphones or headphones using a web based application


1)Download the zip file, unzip it 
2)run the cmd "pip install flask miniaudio numpy sounddevice" in the project file from the command prompt and run app.py
3)This starts a local dev server that allows u to open the page itself
4)Open up the global port: http://127.0.0.1:5000
5)Thats it now enjoy....
6)Just use the given audio files or download your own to the file and just type the name of the file in or if it isn't in the folder, just past the file path

ps: this is one of the easier projects don't judge..
